Почему говорят, что это "как Redux"?

Потому что тут есть те же элементы:
| Redux | useReducer | Описание |
|----------------------|------------------------|----------|
| **store** | **state** | Хранит текущее состояние |
| **reducer** | **reducer** | Чистая функция, описывающая, как меняется состояние |
| **dispatch(action)** | **dispatch(action)** | Способ отправить действие, которое изменит состояние |
| **actions** | **actions** | Объекты с полем `type` (и иногда `payload`), описывающие изменения |
| **pure reducer** | **pure reducer** | Оба используют чистые функции для обновления состояния |
| **глобальный state** | **локальный state** | Redux — глобальный для всего приложения, useReducer — локальный |
| **middleware** | **нет middleware** | Redux поддерживает middleware (thunk, saga), useReducer — нет |
| **DevTools** | **нет DevTools** | В Redux есть Redux DevTools, в useReducer — нет |
| **много компонентов**| **один компонент / Context** | Redux легко шарит state между компонентами, useReducer — только через Context |

Разница:

Redux управляет глобальным состоянием приложения

useReducer управляет локальным состоянием одного компонента (или нескольких, если его передавать через Context)

🟦 Когда использовать useReducer?

Использовать лучше, чем useState, когда:

✅ Много логики обработки состояния
✅ Много различных действий
✅ Состояние сложное
✅ Нужно повторно использовать одну и ту же логику
✅ Вы не хотите подключать Redux ради мелочи

Например:

формы с множеством полей

шаги в мастере (много переходов)

сложные объекты состояния

игры (напр. крестики-нолики)

фильтры (сортировка, поиск, выбор категорий и т.д.)

🟥 Когда НЕ стоит использовать useReducer?

❌ Когда состояние простое (useState легче)
❌ Когда нужен глобальный state между многими компонентами → лучше Redux Toolkit
❌ Когда нужен серверный state (загрузки, ошибки, кэширование) → лучше React Query

Context — хорошо для:

выбранной темы (dark/light)

авторизации пользователя

локального состояния для небольшой части приложения

Redux — для:

большого приложения с многоуровневой логикой

сложных reducer’ов

сложных async операций (загрузка, ошибки, кэширование и т.д.)
